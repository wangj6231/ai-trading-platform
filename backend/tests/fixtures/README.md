# Test fixtures

Everything in this directory is synthetic or captured provider-shape data used only by automated tests. It must never be returned as production market data or a production trading signal.

